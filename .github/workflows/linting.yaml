name: Linting

on: pull_request

jobs:
  jest:
    name: Biome
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: v22.20.0

      - run: npm ci

      - run: npm run lint
